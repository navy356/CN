#include <ncurses.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    /*initscr();
    cbreak();
    noecho();
    clear();
    refresh();

    move(5, 5);

    char * msg = "Hello world!";
    for (int i = 0; i < strlen(msg); i++)
    {
        addch(msg[i]);
        addch(' ');
    }

    refresh();

    while(1);*/
    
}